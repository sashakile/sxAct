#!/usr/bin/env python3
"""
Example of using Wolfram Client for Python to interact with Wolfram Engine
Note: This requires a running Wolfram Kernel with network access
"""

# Start a Wolfram Language session
# For now, this is just a template - actual connection would require
# a running Wolfram Kernel accessible via network
print("Wolfram Client for Python example")
print("=" * 50)

# Example code structure (commented out until kernel is accessible)
# with WolframLanguageSession() as session:
#     # Evaluate simple expression
#     result = session.evaluate('2 + 2')
#     print(f"2 + 2 = {result}")
#
#     # Load xAct
#     session.evaluate(wlexpr('AppendTo[$Path, "/opt"]'))
#     session.evaluate(wlexpr('Needs["xAct`xCore`"]'))
#     print("xAct loaded successfully!")

print("\nTo use Wolfram Client for Python:")
print("1. Start a Wolfram Kernel with network access")
print("2. Uncomment the code above and configure the connection")
print("3. See: https://reference.wolfram.com/language/WolframClientForPython/")
