# Binder configuration: use the pre-built GHCR image so Binder does not
# need to compile Julia packages on demand (which OOMs on Binder's 2 GB limit).
#
# The image is built and pushed on every GitHub release via
# .github/workflows/release-container.yml
FROM ghcr.io/sashakile/xact.jl:dev
